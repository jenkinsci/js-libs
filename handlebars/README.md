Jenkins CI Handlebars runtime modules (Browserified).

__NOTE__:

* This module needs to be published to npm.
* Remove the __target__ folder before publishing. Otherwise, be prepared for uploading of a huge package.